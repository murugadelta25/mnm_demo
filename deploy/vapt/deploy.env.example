# Copy to deploy/vapt/deploy.env and edit before starting the VAPT instance.
# Do NOT point these values at production.

CLIENT_NAME=vapt
DB_USER=root
DB_PASS=ChangeMe-VaptDb-2026!
DB_NAME=eap_pms_vapt

BACKEND_PORT=8020
FRONTEND_PORT=5274

INSTALL_SYSTEMD=no

# Domain mapping: see deploy/vapt/domain.config.json (vapt.eappms)
