/**
 * Navigation — built from feature-registry.json (see config/featureRegistry.js).
 *
 * To add a page: edit feature-registry.json + App.jsx route only.
 */
export { buildNavigation as getNavigationForRole } from './config/featureRegistry';
